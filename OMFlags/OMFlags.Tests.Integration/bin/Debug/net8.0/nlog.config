<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="info"
      internalLogFile="L:\var\log\MeterStoreApiLogs\internal-nlog.txt">

  <!-- Load the ASP.NET Core plugin -->
  <extensions>
    <add assembly="NLog.API.AspNetCore"/>
  </extensions>

  <!-- the targets to write to -->
  <targets>
    <!-- write logs to file  -->
    <target xsi:type="File" name="MeterStore.Api-All"
            fileName="L:/var/log/MeterStoreApiLogs/All.log"
            archiveFileName="L:/var/log/MeterStoreApiLogs/archive/All.{#}.log"
            archiveNumbering="DateAndSequence"
            archiveDateFormat = "yyyyMMdd"
            archiveEvery="Day"
            archiveAboveSize="100000000"
            maxArchiveFiles="10"
            layout="${longdate}|${uppercase:${level}}|${logger}|${message}${onexception:|${event-properties:item=EventId_Id}|${exception:format=ToString}|${callsite}}"/>
    <!-- write logs to file  -->
    <target xsi:type="File" name="MeterStore.Api-Trace"
            fileName="L:/var/log/MeterStoreApiLogs/Trace.log"
            archiveFileName="L:/var/log/MeterStoreApiLogs/archive/Trace.{#}.log"
            archiveNumbering="DateAndSequence"
            archiveDateFormat = "yyyyMMdd"
            archiveEvery="Day"
            archiveAboveSize="100000000"
            maxArchiveFiles="10"
            layout="${longdate}|${uppercase:${level}}|${logger}|${message}${onexception:|${event-properties:item=EventId_Id}|${exception:format=ToString}|${callsite}}"/>
    <!-- write logs to file  -->
    <target xsi:type="File" name="MeterStore.Api-Info"
            fileName="L:/var/log/MeterStoreApiLogs/Info.log"
            archiveFileName="L:/var/log/MeterStoreApiLogs/archive/Info.{#}.log"
            archiveNumbering="DateAndSequence"
            archiveDateFormat = "yyyyMMdd"
            archiveEvery="Day"
            archiveAboveSize="100000000"
            maxArchiveFiles="30"
            layout="${longdate}|${uppercase:${level}}|${logger}|${message}${onexception:|${event-properties:item=EventId_Id}|${exception:format=ToString}|${callsite}}"/>
    <!-- write logs to file  -->
    <target xsi:type="File" name="MeterStore.Api-Error"
            fileName="L:/var/log/MeterStoreApiLogs/Error.log"
            archiveFileName="L:/var/log/MeterStoreApiLogs/archive/Error.{#}.log"
            archiveNumbering="DateAndSequence"
            archiveDateFormat = "yyyyMMdd"
            archiveEvery="Day"
            archiveAboveSize="100000000"
            maxArchiveFiles="60"
            layout="${longdate}|${uppercase:${level}}|${logger}|${message}${onexception:|${event-properties:item=EventId_Id}|${exception:format=ToString}|${callsite}}"/>
  </targets>

  <!-- rules to map from logger name to target -->
  <rules>
    <!--All logs, including from Microsoft-->
    <logger name="*" minlevel="Trace" writeTo="MeterStore.Api-All" />
    <!--Skip non-critical Microsoft logs and so log only own logs-->
    <logger name="Microsoft.*" maxLevel="Info" final="true" />
    <logger name="*" minlevel="Trace" writeTo="MeterStore.Api-Trace" />
    <logger name="*" minlevel="Info" writeTo="MeterStore.Api-Info" />
    <logger name="*" minlevel="Error" writeTo="MeterStore.Api-Error" />
  </rules>
</nlog>